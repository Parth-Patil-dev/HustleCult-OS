import TimelineCard from "@/features/opportunities/components/details/TimelineCard";

function TimelineTab({ opportunity }) {
  return (
    <TimelineCard opportunity={opportunity} />
  );
}

export default TimelineTab;